"use strict";

let hasDriverLicense = false;
const passTest = true;

if(passTest) hasDriverLicense = true;
if(hasDriverLicense) console.log("You can Drive 😉!");

// Canot use the reserved keywords as variable
const private = 10;